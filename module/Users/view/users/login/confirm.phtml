<section class="lof=gin-confirm">
    <h2>Login successfull</h2>
    <p>Thank you!</p>
    <p>Your data:</p>
    <?php foreach ($this->data as $key => $value): ?>
        <?php if (in_array($key, ['password', 'submit'])) continue; ?>
        <?= "$key - $value"; ?><br>
    <?php endforeach; ?>
</section>